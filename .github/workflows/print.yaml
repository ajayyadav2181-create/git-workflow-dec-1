name: Print Message
on:
  push:
    branches:
      -main

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      -name: Print a Message
        run: echo "Hello a Push detected on main branch"


